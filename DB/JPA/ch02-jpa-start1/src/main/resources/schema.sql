CREATE TABLE MEMBER (
                        id VARCHAR(255) NOT NULL,
                        name VARCHAR(255),
                        age INTEGER NOT NULL,
                        PRIMARY KEY(id)
);